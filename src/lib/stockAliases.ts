// 종목 구(舊) 사명 별칭 레지스트리 — 통제된 SEO alias 전용 (Slice H).
//
// 계약(SOURCE OF TRUTH):
//   - 여기 값은 오직 "공개 기록으로 확인된 과거 정식 사명"만 담는다. 추정·비공식 약칭·
//     테마성 별명은 금지한다(재감사 불변식: 미확인은 넣지 않는다).
//   - 이 별칭들은 canonical 표시 이름(stocks.json 의 s.name)을 **절대 대체하지 않는다.**
//     페이지 제목, canonical URL, 주요 메타 설명에는 들어가지 않는다.
//   - 노출 위치는 구조화 데이터(JSON-LD)의 alternateName 처럼 "별칭 전용" 필드 한 곳뿐이다.
//     목적은 구 사명으로 검색한 사용자가 현재 종목 페이지를 찾을 수 있게 하는 것.
//   - 신규 항목 추가는 확인된 개명만 허용한다. 현재 사명과 같은 문자열을 별칭으로 넣지 않는다
//     (그러면 "stale canonical naming" 이 된다 — 검증기가 거부).
//
// key = 종목 코드(ticker), value = 과거 정식 사명 목록(최근 → 과거 순).
export const FORMER_NAME_ALIASES: Readonly<Record<string, readonly string[]>> = {
  "352820": ["빅히트엔터테인먼트", "빅히트"], // 하이브 (2021 사명 변경)
  "097230": ["한진중공업"], // HJ중공업 (2021 사명 변경)
  "005440": ["현대그린푸드"], // 현대지에프홀딩스 (2023 지주 전환)
  "139130": ["DGB금융지주"], // iM금융지주 (2024 사명 변경)
  "267250": ["현대중공업지주"], // HD현대 (2022 사명 변경)
  "267260": ["현대일렉트릭"], // HD현대일렉트릭 (2023 사명 변경)
  "329180": ["현대중공업"], // HD현대중공업 (2023 사명 변경)
  "298050": ["효성첨단소재"], // HS효성첨단소재 (2025 사명 변경)
};

/**
 * 통제된 구 사명 별칭 목록. 없으면 빈 배열.
 * canonical 이름을 대체하는 용도로 쓰지 말 것 — alternateName 전용.
 */
export function formerNamesOf(ticker: string): readonly string[] {
  return FORMER_NAME_ALIASES[ticker] ?? [];
}
